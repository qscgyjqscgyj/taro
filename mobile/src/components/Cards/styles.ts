import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    cardsWrapper: {
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'wrap',
        justifyContent: 'flex-start',
        paddingTop: 16,
        paddingLeft: 14,
        paddingRight: 14,
    },
});
