import { NativeStackScreenProps } from '@react-navigation/native-stack';

import { RootStackParamList } from 'src/components/App/types';

export type SetScreenProps = NativeStackScreenProps<RootStackParamList, 'Set'>;
